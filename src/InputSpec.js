export class InputSpec
{

    constructor(name,inputs,ancestors,nodeGuid)
    {
        this.name = name;
        this.inputs = inputs;
        this.ancestors = ancestors;
        this.nodeGuid = nodeGuid;
    }
}